version: '3.8'

services:
  clh-grpc-auth:
    image: glad2os/clh-auth
    ports:
      - "8080:8080"
    environment:
      - REDIS_HOST=localhost:6379
    links:
      - redis

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
